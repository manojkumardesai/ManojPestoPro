// TO DO: The below function reverses only ASCI supported format, need to make it independent

function reverse(str) {
    return str.split("").reverse().join("");
}

// TO DO: The below function equals only ASCI supported format, need to make it independent

function equal(firstString, secondString) {
    return firstString === secondString;
}